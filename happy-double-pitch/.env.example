# Required by api/send-email.js. Get a key at https://resend.com after
# creating a free account, then add it as an environment variable in the
# Vercel project settings (Settings -> Environment Variables) — do not
# commit the real value.
RESEND_API_KEY=
